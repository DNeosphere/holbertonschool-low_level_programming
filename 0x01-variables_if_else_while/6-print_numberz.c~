#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
int codeA
for (codeA = 48; codeA <= 57; codeA++)
{
putchar(codeA);
}
putchar('\n');
return (0);
}
