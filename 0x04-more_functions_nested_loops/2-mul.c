#include "holberton.h"
/**
 * mul - multiplies two digits
 * @a: num a
 * @b: num b
 * Return: the operation btween both digits
 *
 */
int mul(int a, int b)
{
int mult;
mult = (a * b);
return (mult);
}
