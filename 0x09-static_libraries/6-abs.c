#include "holberton.h"
/**
 * _abs - returns the absolute value of a number
 * @n: - is the number
 * Return: absolute value
 */
int _abs(int n)
{
int b;
int c;
b = -1;
c = n * b;
if (n > 0 || n == 0)
{
return (n);
}
else
{
return (c);
}
}
